<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
	"-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="_4.mapper.OwnerMapper">
		<insert id="ownerForm" parameterType="ownerDTO">
			insert into owner 
			(OWNER_NUM, OWNER_ID, OWNER_PW, OWNER_NAME
			, OWNER_BIRTH, OWNER_EMAIL, OWNER_PHONE)
			values ('1', #{ownerId}, #{ownerPw}, #{ownerName}
			,#{ownerBirth} , #{ownerEmail}, #{ownerPhone})
		</insert>
	</mapper>